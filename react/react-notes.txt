for creating a React project:
1) npm create vite@latest <project name>
2) npm install
3) npm run dev

1) BootStrap:

  1) install Bootstrap dependency 
  2) delete all .css files and remove the imports
  3) import 'bootstrap/dist/css/bootstrap.min.css' in main.jsx

2) Fragment:
   instead of wrapping the component in div tag we can use fragment tag
   return <>
    ...
    </>

    it is represented as <> .. </>

3) while rendering a list use the map method and use the key attribute which uniquely identifies each item:

  let list = []

  return <>
  <ul className="list-group">
  <li className="list-group-item active" aria-current="true">Header</li>
  {
    healthy.map(
      (item,index)=> <li key={index} className="list-group-item">{item}</li>
    )
  }

4) Properties/Props: it is a way of passing data from parent component to child component.
  it is a key value pair of passing data.

  1) Taking props as a input parameter.

  funciton Child(props){  // take props as a input parameter
    return <>
      {console.log(props.attribute)}
      </>
  } 

  function Parent(){
    <Child attribute={data}/>

  }

  here the key which is used while calling the Child component is very important, because we have to use the same key while fetching the data in child component: props.attribute. 

  2) using Object Destructuring:

  The Parent component remains same, changes are to be made in the child component.

    funciton Child({attribute}){  // destructure the props object and take attribute out of it.
    return <>
      {console.log(attribute)}
      </>
  }

5)  Modular css: it is used when we have a single .css file and there are multiple styles with the same name.
So what to do in this situation: 
1) Create seprate css files for each component and follow the compulsory naming convention: Component.modular.css;
2) import in the .jsx file where it will be used, for eg: import style from './..Component.modular.css'
3) use it in the component where it is required: `{style['style-name']}`

6) onClick event:

every jsx element has a attribute onClick which requires a arrow function.

let function = ()=>{
  ...implementation
}

<div onClick=function>
<div/>

OnChange event: it is used when the user is entering the input and we have to show it changing dynamically.

let function = (event)=>{
  console.log(event.target.value) // shows the current value.
}

call it normally, no need to pass the event parameter.

we can also pass fucntions to child component via props, this is mainly used so that the child component will only show the data and use the functionality/behaviour given by the parent.

7) State Management:

meaning: the component rerenders when there is a change in the data.
it is done using a Hook. which is a js function which is used in a functional component.

1) useState:

let [getValue,setValue] = useState("initial value")

for getting the current value: getValue
for entering the new data: setValue(value)

8) Context-Api: context api is a centralised place where the states/data will be saved and we donot need to pass prompt to avoid prop drilling.
which ever component requires the data it can get it from Context api.
The main reason is avoid passing of props because it might be un necessary.

it uses a special hook useContext.

steps: 

1) create a file for context:

import { createContext } from "react";

export const TodoContext = createContext(initial-value);

  2) than in a parent component which might contain all data and pass to the child component.

    Parent Component

    Data processing.

    <ParentComponent.Provider value={
      {  // data to be passed to child components
      key:{value},
      key:{value},
      key:{value}
      }
    }>

    <>
    Screen components
    </>

    </ParentComponent.Provider>

  3)Child Component:

  let storeObject = useContext(TodoContext) get the Context object.
  storeObject.key;

9) Async Await & Promise: (Api Call)

while using async and await it always returns Promises.
and we have to use .then() to consume the Promise.

while using async await:
  1) use fetch("url") // this returns the Promise which contains many things statusCode, data etc.
  it retunrs the data into Json object which has to be converted into Js object.

  2) fetch(url)
      .json() // this converts the json object to js object.

  the data taht .json() gives is the actual result. which 

  while using Promise:

  1) use fetch("url") this returns Promise and to consume it use .then()

  fetch("url")
    .then(
      (response) => response.json() // converts data to js object
    )
    .then(
      (response) => console.log(response) //actual data
    )

    we just have to use Promise chaining.


10) useEffect Hook:

this hook is used when we want to execute the function on a specific condition, mostly state change.
this hook excepts 2 parameters, 1 parameter is always a function to be executed and 2nd one is a variable parameter.

useEffect(
  ()=>{

    return ()=>{
      implementation of function...
    };

  },
  parameter2
)

if we pass '[]' than the function will only be executed once , when first time the component is rendered.
if passed nothing than every time the component is rendered it will be called.
if we pass the state than whenever the state is changed than the fucntion is called.

we can also write a return function at the end of the useEffect block which is basically called when the component is about to die.

while making api call we have to use the below 

let controller = new AbortController()
let signal = controller.signal

useEffect(
  {

    logic for api call

    
return ()=>{
  controller.abort() // aborts the api call if made 
}

  },
  parameter
)



11) Shortcut: when we have to render a ui component just on the basis of 1 condition

12) making api call for other methods: when we donot pass any method , by default is is GET.

fetch(
  'url',{ // other required fields.

  method:'POST/DELETE/PUT',
  headers:{
    'content-type':'application-json'
  },
  body:{ request body

    }

  }
).then(
  (response)=>response.json() 
).then(
  (response)=>consume the data.
)

{ condition && <Component/> }

basically it is like A && B , when the condition is true than it returns B, and if the condition is false it returns false.


13) Routing:

1) install the dependency: npm install react-router-dom

2) Do this steps in Main.jsx
sometimes we get problem in auto complete so first import 

import {createBrowserRouter, RouterProvider} from 'react-router-dom'

const router = createBrowserRouter(   Basic Structure
  [
    {path:"/route",element:<Component/>}
  ]
)

<RouterProvider router={router}>

</RouterProvider>

now if we direct put component in the element it will only render the component and not the entire screen so for that , 

const router = createBrowserRouter(
  [
    {path:"/path",

    element:<Parent/>,

    child:{
      path:"path1", element:<Parent/>,
      path:"path2", element: <Child2/>,
      path:"path3", element:<Child3/>
    }
    }
  ]
)

and where we use the ternary operator for showing the Component we have to use <Outlet/>.

this means that for eg: there is a fragment and we have to show different components on different routes keeping the Header and Footer intact so use this method.


4) Loader & Action:

loader: loader takes a function which runs before rendering the Component. and it is written in router.
action: action is a function which is executed when a form is submit

[
  {
    path:"/path",
    element: <Component/>,
    loader:{
      ()=>{

      }
    },
    action:{
      ()=>{
        
      }
    }
  }
]


2) How to Navigate from one component to another without onClick{},

so use <Link to="/path" >
  <Component/>
  </Link>

in this there is no need of onClick.

3) even better we can use navigate("/path")

let navigate = useNavigate() // import this.

onClick={
  navigate("/path")
}




